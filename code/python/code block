# guessing game
import random
def pick_a_number(X):
    random_number = random.randint(1,X)
    while pick_a_number != random.number:
        pick_a_number = int(input(f'Guess a number between 1 and {x}:'))
        if pick_a_number < random_number:
            print ('Pick a bigger number.')
        else:
            print ('pick a smaller number.')

print (f'You have guessed the number{random_number}')
pick_a_number
#while number_of_guesses < 5:
    guess = int(input())
    number_of_guesses += 1
    if guess < number:
        print('Your guess is too low')
    if guess > number:
        print('Your guess is too high')
    if guess == number:
        break